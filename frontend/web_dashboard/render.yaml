services:
  - type: static
    name: ai-alshehri-web
    runtime: static
    rootDir: frontend/web_dashboard
    buildCommand: npm ci && npm run build
    staticPublishPath: build
    autoDeploy: true
    envVars:
      - key: REACT_APP_API_URL
        sync: false
